export enum UserStatus {
  Unverified = 'unverified',
  Verified = 'verified',
  Active = 'active',
  Locked = 'locked',
  Suspended = 'suspended',
  Deleted = 'deleted',
  Blocked = 'blocked'
}

export enum UserRole {
  User = 'user',
  Admin = 'admin'
}
